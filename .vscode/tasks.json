{
    "version": "2.0.0",
    "tasks": [
        {
            // Default build task, press Ctrl+Shift+B to build dev version and launch
            "label": "HEMTT Launch",
            "command": "hemtt.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "launch"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
